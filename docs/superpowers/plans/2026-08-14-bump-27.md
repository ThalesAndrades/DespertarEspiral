# Bump de R$ 27 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Uma caixa marcável no checkout ("Mapa dos Sentimentos que Aprisionam — +R$ 27") que soma ao total e, no pagamento confirmado, libera **dois** produtos em vez de um.

**Architecture:** O pedido passa a poder ter mais de um item através de uma tabela nova `order_items` (order_id, product_id, unit_price). `orders.product_id` continua preenchido com o produto **principal** — nada do que lê essa coluna hoje quebra. A liberação de acesso (`asaas-webhook`, `grant-pending-access`) percorre `order_items` quando existirem e cai de volta em `orders.product_id` quando não existirem, o que mantém todo pedido antigo funcionando. A regra de preço e de elegibilidade do bump é calculada **no servidor** a partir do banco: o cliente só manda um booleano, nunca um valor.

**Tech Stack:** React 18 + TypeScript + Vite · Supabase (Postgres + Edge Functions em Deno) · vitest + @testing-library/react · gateway de pagamento **Asaas** (medido: `ASAAS_API_KEY` em `checkout-session`, liberação por `asaas-webhook`).

## Global Constraints

Valores copiados da spec `docs/superpowers/specs/2026-07-31-fase-0-esteira-design.md`. Todo task herda esta seção.

- **Produto `em_breve` NUNCA aceita pagamento** (spec §3.6, "regra dura"). Vale também para o bump: se o produto do bump não estiver `disponivel`, a caixa não aparece e a edge function recusa o item.
- **A trava real é a edge function**, o filtro do front é apresentação. Padrão já estabelecido em `supabase/functions/checkout-session/index.ts:225-233`.
- **Preço nunca vem do cliente.** O total é recalculado no servidor a partir de `products.price`. O cliente manda `bump: true|false`.
- **Copy em pt-BR**, com acentuação correta, tom acolhedor, sem cobrança.
- **Design system:** só tokens CSS (`var(--gold)`, `var(--text-muted)`, `var(--space-*)`, …), **zero hex literal** em código novo. Referência: `DESIGN_SYSTEM.md`.
- **Sem escassez falsa** (spec §3.5): nada de contador, "restam N vagas" ou preço-âncora que nunca existiu.
- **Toda tabela nova nasce com RLS habilitada**, no padrão de `supabase/migrations/20260414_000001_production_rls.sql`.
- **Migração idempotente:** `if not exists` em tudo; reaplicar não pode corromper dado.
- **Não tocar em `src/pages/__tests__/CheckoutPage.test.tsx`** (14 casos, verde). Teste novo vai em arquivo novo — mesma convenção da leva Sete Manhãs.
- **Comportamento sem bump é intocável.** Pedido de um item continua liberando um produto (proteção contra regressão — spec §8).

---

## Contexto medido (14/08/2026)

Fatos verificados nesta máquina, não lembrados. Quem for implementar pode confiar nestes:

| Fato | Onde |
|---|---|
| Produto do bump **já existe no seed**: slug `mapa-dos-sentimentos`, R$ 27, status `em_breve` | `supabase/migrations/20260731_000002_seed_esteira.sql:21-23` |
| `order_items` **não existe** em lugar nenhum do repo | `grep -rn order_items` → 0 resultados |
| Gateway ativo é **Asaas** | `supabase/functions/checkout-session/index.ts:272` |
| `asaas-webhook` **não compara** valor pago com `orders.amount` — um total maior não é rejeitado | `supabase/functions/asaas-webhook/index.ts:126-143` |
| Liberação lê `order.product_id` em 3 pontos | `asaas-webhook/index.ts:153,169` · `grant-pending-access/index.ts:116` |
| **Não há teste de edge function** no repo (Deno). Lógica pura testável mora em `src/lib/*.ts` com vitest | `find supabase -name "*.test.ts"` → vazio |
| Botão de pagar (onde a caixa entra logo acima) | `src/pages/CheckoutPage.tsx:523` |
| Corpo enviado à edge function | `src/pages/CheckoutPage.tsx:122-130` |
| Resumo com "Total" | `src/pages/CheckoutPage.tsx:624` |
| Idiom de RLS, inclusive `public.is_admin()` | `supabase/migrations/20260414_000001_production_rls.sql:35-45` |

**Consequência prática do primeiro fato:** com o bump `em_breve`, a caixa **não vai aparecer** depois desta implementação. Isso é o comportamento correto e honesto, não um bug. Ela acende sozinha quando a Sunyan entregar o conteúdo e o status virar `disponivel`. Os testes cobrem os dois estados.

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `supabase/migrations/20260814_000001_order_items.sql` **(criar)** | Tabela `order_items` + RLS + índices. Nada mais. |
| `src/lib/bump.ts` **(criar)** | Regra pura: slug do bump, elegibilidade, cálculo do total. Sem React, sem Supabase — 100% testável. Mesmo padrão de `src/lib/seteManhas.ts`. |
| `src/lib/__tests__/bump.test.ts` **(criar)** | Testes da regra pura. |
| `src/pages/CheckoutPage.tsx` **(modificar)** | Busca o produto do bump, renderiza a caixa, soma no resumo, manda `bump` no corpo. |
| `src/pages/__tests__/CheckoutPage.bump.test.tsx` **(criar)** | Integração da caixa. Arquivo novo — não encostar no existente. |
| `supabase/functions/checkout-session/index.ts` **(modificar)** | Revalida o bump, calcula o total no servidor, grava `order_items`. |
| `supabase/functions/_shared/orderItems.ts` **(criar)** | Helper compartilhado pelas duas funções de liberação: dado um pedido, devolve os produtos dele (com fallback). |
| `supabase/functions/asaas-webhook/index.ts` **(modificar)** | Libera N produtos em vez de 1. |
| `supabase/functions/grant-pending-access/index.ts` **(modificar)** | Idem, no caminho de conta criada depois do pagamento. |

---

## Task 1: Migração `order_items`

**Files:**
- Create: `supabase/migrations/20260814_000001_order_items.sql`

**Interfaces:**
- Consumes: nada.
- Produces: tabela `public.order_items (id uuid pk, order_id uuid not null → orders, product_id uuid not null → products, unit_price numeric not null, created_at timestamptz)`, com índice único `(order_id, product_id)`. Tasks 4 e 5 dependem exatamente destes nomes.

**Sem teste automatizado:** o repo não tem harness de SQL. A verificação é leitura + idempotência (Step 2). Aplicar em produção é gate do Thales, **não** faz parte deste plano.

- [ ] **Step 1: Escrever a migração**

```sql
-- Um pedido pode conter mais de um produto (bump de R$ 27).
-- orders.product_id CONTINUA sendo o produto principal: tudo que ja le essa
-- coluna (asaas-webhook, grant-pending-access, order-recovery) segue valido.
-- order_items e aditivo — pedido antigo, sem itens, tem fallback no codigo.

create table if not exists public.order_items (
  id          uuid primary key default gen_random_uuid(),
  order_id    uuid not null references public.orders(id)   on delete cascade,
  product_id  uuid not null references public.products(id) on delete restrict,
  unit_price  numeric(10,2) not null check (unit_price >= 0),
  created_at  timestamptz not null default now()
);

-- Idempotencia do gravador: reprocessar um pedido nao duplica item.
create unique index if not exists uniq_order_items_order_product
  on public.order_items (order_id, product_id);

-- A liberacao de acesso percorre os itens de UM pedido: este e o caminho quente.
create index if not exists idx_order_items_order
  on public.order_items (order_id);

alter table public.order_items enable row level security;

-- Mesma regra de orders: a aluna ve os itens do proprio pedido; admin ve tudo.
-- Escrita e exclusiva do service_role (edge functions), que ignora RLS —
-- por isso NAO existe policy de insert/update/delete aqui, de proposito.
drop policy if exists "order_items_select_own_or_admin" on public.order_items;
create policy "order_items_select_own_or_admin"
on public.order_items
for select
to authenticated
using (
  exists (
    select 1 from public.orders o
     where o.id = order_items.order_id
       and (o.user_id = auth.uid() or public.is_admin())
  )
);
```

- [ ] **Step 2: Verificar idempotência e ordem de nomes por leitura**

Rodar (não aplica nada, só confere que o arquivo existe e que todo objeto criado é guardado):

```bash
grep -c "if not exists" supabase/migrations/20260814_000001_order_items.sql
```

Esperado: `3` (table + 2 índices). A policy usa `drop policy if exists` antes do `create` — mesmo padrão de `20260731_000001_storefront.sql:40-43`.

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/20260814_000001_order_items.sql
git commit -m "feat(bump): migracao order_items - pedido com mais de um produto"
```

---

## Task 2: Regra pura do bump (`src/lib/bump.ts`)

**Files:**
- Create: `src/lib/bump.ts`
- Test: `src/lib/__tests__/bump.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces (Tasks 3 e 4 usam estes nomes exatos):
  - `BUMP_SLUG: "mapa-dos-sentimentos"`
  - `interface ProdutoBump { id: string; slug: string; title: string; price: number; is_active?: boolean; status?: string }`
  - `bumpOferecivel(bump: ProdutoBump | null | undefined, slugDoCheckout: string | undefined): boolean`
  - `calcularTotal(precoProduto: number, precoBump: number | null | undefined, marcado: boolean): number`

- [ ] **Step 1: Escrever os testes que falham**

Criar `src/lib/__tests__/bump.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { BUMP_SLUG, bumpOferecivel, calcularTotal, type ProdutoBump } from "@/lib/bump";

const bumpOk: ProdutoBump = {
  id: "bump-1", slug: BUMP_SLUG, title: "Mapa dos Sentimentos que Aprisionam",
  price: 27, is_active: true, status: "disponivel",
};

describe("bumpOferecivel", () => {
  it("oferece quando o produto esta ativo, disponivel e com preco", () => {
    expect(bumpOferecivel(bumpOk, "sete-manhas")).toBe(true);
  });

  it("NAO oferece quando o bump esta em_breve (regra dura: em_breve nao vende)", () => {
    expect(bumpOferecivel({ ...bumpOk, status: "em_breve" }, "sete-manhas")).toBe(false);
  });

  it("NAO oferece quando o bump esta inativo", () => {
    expect(bumpOferecivel({ ...bumpOk, is_active: false }, "sete-manhas")).toBe(false);
  });

  it("NAO oferece quando o produto do checkout E o proprio bump (nao vender duas vezes)", () => {
    expect(bumpOferecivel(bumpOk, BUMP_SLUG)).toBe(false);
  });

  it("NAO oferece quando o bump nao existe ou tem preco invalido", () => {
    expect(bumpOferecivel(null, "sete-manhas")).toBe(false);
    expect(bumpOferecivel(undefined, "sete-manhas")).toBe(false);
    expect(bumpOferecivel({ ...bumpOk, price: 0 }, "sete-manhas")).toBe(false);
    expect(bumpOferecivel({ ...bumpOk, price: Number.NaN }, "sete-manhas")).toBe(false);
  });
});

describe("calcularTotal", () => {
  it("sem bump marcado, o total e o preco do produto", () => {
    expect(calcularTotal(47, 27, false)).toBe(47);
  });

  it("com bump marcado, soma os dois", () => {
    expect(calcularTotal(47, 27, true)).toBe(74);
  });

  it("bump marcado sem preco de bump nao altera o total (defensivo)", () => {
    expect(calcularTotal(47, null, true)).toBe(47);
    expect(calcularTotal(47, undefined, true)).toBe(47);
  });

  it("arredonda em centavos — nunca devolve 75.39999999999999", () => {
    expect(calcularTotal(47.9, 27.5, true)).toBe(75.4);
  });

  it("preco de produto invalido nunca vira NaN no total", () => {
    expect(calcularTotal(Number.NaN, 27, true)).toBe(0);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
NODE_OPTIONS=--max-old-space-size=4096 npx vitest run src/lib/__tests__/bump.test.ts
```

Esperado: FAIL — `Failed to resolve import "@/lib/bump"`.

- [ ] **Step 3: Implementar o mínimo**

Criar `src/lib/bump.ts`:

```ts
/**
 * Bump de R$ 27 — regra pura, sem React e sem Supabase.
 *
 * Espelha a validacao que a edge function `checkout-session` faz do lado do
 * servidor. Aqui e APRESENTACAO (decide se a caixa aparece); la e a TRAVA
 * (decide se cobra). As duas precisam concordar, mas a do servidor e a que
 * vale — mesmo padrao ja usado para `status = disponivel` no catalogo.
 */

export const BUMP_SLUG = "mapa-dos-sentimentos";

export interface ProdutoBump {
  id: string;
  slug: string;
  title: string;
  price: number;
  is_active?: boolean;
  status?: string;
}

/**
 * O bump so pode ser oferecido quando existe, esta ativo, esta `disponivel`,
 * tem preco positivo e NAO e o proprio produto que ja esta sendo comprado
 * (senao a aluna pagaria duas vezes pela mesma coisa).
 */
export function bumpOferecivel(
  bump: ProdutoBump | null | undefined,
  slugDoCheckout: string | undefined
): boolean {
  if (!bump) return false;
  if (bump.slug === slugDoCheckout) return false;
  if (bump.is_active === false) return false;
  if (bump.status !== "disponivel") return false;
  return Number.isFinite(bump.price) && bump.price > 0;
}

/** Soma em centavos e volta para reais: evita 47.9 + 27.5 = 75.39999999999999. */
export function calcularTotal(
  precoProduto: number,
  precoBump: number | null | undefined,
  marcado: boolean
): number {
  const base = Number.isFinite(precoProduto) ? precoProduto : 0;
  const extra = marcado && Number.isFinite(precoBump as number) ? (precoBump as number) : 0;
  return Math.round((base + extra) * 100) / 100;
}
```

- [ ] **Step 4: Rodar e ver passar**

```bash
NODE_OPTIONS=--max-old-space-size=4096 npx vitest run src/lib/__tests__/bump.test.ts
```

Esperado: PASS, 11 testes.

- [ ] **Step 5: Commit**

```bash
git add src/lib/bump.ts src/lib/__tests__/bump.test.ts
git commit -m "feat(bump): regra pura de elegibilidade e total"
```

---

## Task 3: A caixa no `CheckoutPage`

**Files:**
- Modify: `src/pages/CheckoutPage.tsx` (busca do bump no efeito que carrega o produto; caixa acima de `:523`; total no resumo `:624`; corpo em `:122-130`; analytics em `:156`)
- Create: `src/pages/__tests__/CheckoutPage.bump.test.tsx`

**Interfaces:**
- Consumes: `BUMP_SLUG`, `bumpOferecivel`, `calcularTotal`, `ProdutoBump` de `@/lib/bump` (Task 2).
- Produces: o corpo enviado a `checkout-session` ganha a chave **`bump: boolean`** — Task 4 lê exatamente esse nome.

- [ ] **Step 1: Escrever os testes que falham**

Criar `src/pages/__tests__/CheckoutPage.bump.test.tsx`. Copiar o bloco de mocks de `src/pages/__tests__/CheckoutPage.test.tsx:19-60` (asset, Helmet, SpiralLogo, analytics, sequenzy, toast, useAuth) — **copiar, não importar**, é a convenção do repo. O mock do supabase precisa responder a duas consultas de produto por slug: o produto do checkout e o bump.

```tsx
/**
 * Integration Tests — CheckoutPage · bump de R$ 27
 * Arquivo separado de propósito: CheckoutPage.test.tsx tem 14 casos verdes e
 * não deve ser tocado.
 */
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { MemoryRouter, Route, Routes } from "react-router-dom";
import { BUMP_SLUG } from "@/lib/bump";

/* ...bloco de mocks copiado de CheckoutPage.test.tsx:19-60... */

const PRODUTO = {
  id: "p-1", slug: "sete-manhas", title: "Sete Manhãs", price: 47,
  is_active: true, status: "disponivel",
};
const BUMP_DISPONIVEL = {
  id: "b-1", slug: BUMP_SLUG, title: "Mapa dos Sentimentos que Aprisionam",
  price: 27, is_active: true, status: "disponivel",
};

let bumpNoBanco: Record<string, unknown> | null = BUMP_DISPONIVEL;
const mockInvoke = vi.fn().mockResolvedValue({ data: { orderId: "o-1" }, error: null });

vi.mock("@/lib/supabase", () => ({
  supabase: {
    from: () => ({
      select: () => ({
        eq: (_col: string, val: string) => ({
          eq: () => ({
            maybeSingle: async () => ({
              data: val === BUMP_SLUG ? bumpNoBanco : PRODUTO, error: null,
            }),
            single: async () => ({
              data: val === BUMP_SLUG ? bumpNoBanco : PRODUTO, error: null,
            }),
          }),
          maybeSingle: async () => ({
            data: val === BUMP_SLUG ? bumpNoBanco : PRODUTO, error: null,
          }),
          single: async () => ({
            data: val === BUMP_SLUG ? bumpNoBanco : PRODUTO, error: null,
          }),
        }),
      }),
    }),
    functions: { invoke: (...a: unknown[]) => mockInvoke(...a) },
  },
}));

import CheckoutPage from "@/pages/CheckoutPage";

function renderCheckout(slug = "sete-manhas") {
  return render(
    <MemoryRouter initialEntries={[`/checkout/${slug}`]}>
      <Routes><Route path="/checkout/:slug" element={<CheckoutPage />} /></Routes>
    </MemoryRouter>
  );
}

describe("CheckoutPage — bump de R$ 27", () => {
  beforeEach(() => { bumpNoBanco = BUMP_DISPONIVEL; mockInvoke.mockClear(); });

  it("mostra a caixa do bump quando o produto do bump esta disponivel", async () => {
    renderCheckout();
    expect(await screen.findByLabelText(/Mapa dos Sentimentos que Aprisionam/i)).toBeInTheDocument();
  });

  it("NAO mostra a caixa quando o bump esta em_breve", async () => {
    bumpNoBanco = { ...BUMP_DISPONIVEL, status: "em_breve" };
    renderCheckout();
    await screen.findByText(/Finalizar pedido/i);
    expect(screen.queryByLabelText(/Mapa dos Sentimentos que Aprisionam/i)).not.toBeInTheDocument();
  });

  it("NAO mostra a caixa no checkout do proprio produto do bump", async () => {
    renderCheckout(BUMP_SLUG);
    await screen.findByText(/Finalizar pedido/i);
    expect(screen.queryByLabelText(/Mapa dos Sentimentos que Aprisionam/i)).not.toBeInTheDocument();
  });

  it("marcar a caixa muda o total de 47 para 74", async () => {
    const user = userEvent.setup();
    renderCheckout();
    const caixa = await screen.findByLabelText(/Mapa dos Sentimentos que Aprisionam/i);
    expect(screen.getByTestId("checkout-total")).toHaveTextContent("47,00");
    await user.click(caixa);
    await waitFor(() => expect(screen.getByTestId("checkout-total")).toHaveTextContent("74,00"));
  });

  it("envia bump:true para a edge function quando marcada", async () => {
    const user = userEvent.setup();
    renderCheckout();
    await user.click(await screen.findByLabelText(/Mapa dos Sentimentos que Aprisionam/i));
    await user.type(screen.getByLabelText(/nome/i), "Maria");
    await user.type(screen.getByLabelText(/mail/i), "maria@exemplo.com");
    await user.click(screen.getByRole("button", { name: /Registrar pedido/i }));
    await waitFor(() => expect(mockInvoke).toHaveBeenCalled());
    expect(mockInvoke.mock.calls[0][1].body).toMatchObject({ bump: true });
  });

  it("envia bump:false quando nao marcada (protecao contra regressao)", async () => {
    const user = userEvent.setup();
    renderCheckout();
    await screen.findByLabelText(/Mapa dos Sentimentos que Aprisionam/i);
    await user.type(screen.getByLabelText(/nome/i), "Maria");
    await user.type(screen.getByLabelText(/mail/i), "maria@exemplo.com");
    await user.click(screen.getByRole("button", { name: /Registrar pedido/i }));
    await waitFor(() => expect(mockInvoke).toHaveBeenCalled());
    expect(mockInvoke.mock.calls[0][1].body).toMatchObject({ bump: false });
  });
});
```

**Nota para quem implementa:** o mock do supabase acima é um esqueleto — ajuste o encadeamento (`.eq().eq().single()`) para bater com o que o `CheckoutPage` realmente chama depois da sua edição, e confira o rótulo real do campo de nome/e-mail no arquivo existente (`getByLabelText`) antes de assumir. Se o `findByText(/Finalizar pedido/i)` não existir no seu render, ancore em outro texto estável da página.

- [ ] **Step 2: Rodar e ver falhar**

```bash
NODE_OPTIONS=--max-old-space-size=4096 npx vitest run src/pages/__tests__/CheckoutPage.bump.test.tsx
```

Esperado: FAIL — a caixa não existe.

- [ ] **Step 3: Buscar o produto do bump**

Em `src/pages/CheckoutPage.tsx`, junto ao estado existente (perto de `:43-50`):

```tsx
const [bumpProduct, setBumpProduct] = useState<ProdutoBump | null>(null);
const [bumpMarcado, setBumpMarcado] = useState(false);
```

E, no mesmo `useEffect` que carrega o produto (perto de `:76-90`), depois de setar o produto principal:

```tsx
// Produto do bump: buscado sempre, exibido so se `bumpOferecivel` deixar.
// Falha aqui NUNCA derruba o checkout — sem bump, a compra segue normal.
const { data: bumpData } = await supabase
  .from("products")
  .select("id, slug, title, price, is_active, status")
  .eq("slug", BUMP_SLUG)
  .maybeSingle();

if (bumpData) {
  const bumpPrice = typeof bumpData.price === "number" ? bumpData.price : parseFloat(String(bumpData.price));
  setBumpProduct({ ...(bumpData as ProdutoBump), price: bumpPrice });
}
```

Import no topo:

```tsx
import { BUMP_SLUG, bumpOferecivel, calcularTotal, type ProdutoBump } from "@/lib/bump";
```

- [ ] **Step 4: Renderizar a caixa acima do botão de pagar**

Derivar, junto dos outros derivados (perto de `:347`):

```tsx
const mostrarBump = bumpOferecivel(bumpProduct, slug);
// Se a caixa nao pode ser oferecida, `marcado` nunca conta — nem que o estado
// tenha ficado true por uma troca de produto no meio do caminho.
const bumpAtivo = mostrarBump && bumpMarcado;
const total = calcularTotal(product.price, bumpProduct?.price, bumpAtivo);
```

Inserir **imediatamente antes** do `<button type="submit">` de `:523`:

```tsx
{mostrarBump && bumpProduct && (
  <label
    className="card-dark"
    style={{
      display: "flex", alignItems: "flex-start", gap: "var(--space-3)",
      padding: "var(--space-4)", borderRadius: "var(--r-lg)", cursor: "pointer",
      border: bumpAtivo ? "1px solid var(--gold)" : "1px solid var(--border-subtle)",
      transition: "border-color var(--dur-base) var(--ease-out)",
    }}
  >
    <input
      type="checkbox"
      checked={bumpMarcado}
      onChange={(e) => setBumpMarcado(e.target.checked)}
      aria-label={`${bumpProduct.title} — adicionar por R$ ${bumpProduct.price.toLocaleString("pt-BR", { minimumFractionDigits: 2 })}`}
      style={{ marginTop: "3px", accentColor: "var(--gold)", width: "18px", height: "18px", flexShrink: 0 }}
    />
    <span>
      <span style={{ display: "block", fontSize: "14px", color: "var(--text-primary)", fontWeight: 500 }}>
        {bumpProduct.title}
        <span style={{ color: "var(--gold)" }}> — +R$ {bumpProduct.price.toLocaleString("pt-BR", { minimumFractionDigits: 2 })}</span>
      </span>
      <span className="reading-note" style={{ display: "block", fontSize: "12px", lineHeight: 1.6, marginTop: "var(--space-1)" }}>
        Some ao seu pedido agora e receba junto com o acesso principal.
      </span>
    </span>
  </label>
)}
```

- [ ] **Step 5: Somar no resumo e mandar no corpo**

No `OrderSummary` (`:556` em diante), aceitar o bump e usar `total`. Assinatura nova:

```tsx
function OrderSummary({ product, savings, bump, total }: {
  product: unknown; savings: number;
  bump: { title: string; price: number } | null;
  total: number;
}) {
```

Linha do bump logo acima do "Total" (`:624`) e o valor do total vindo da prop:

```tsx
{bump && (
  <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "var(--space-3)" }}>
    <span className="reading-note" style={{ fontSize: "12px" }}>{bump.title}</span>
    <span style={{ fontSize: "13px", color: "var(--text-primary)" }}>
      + R$ {bump.price.toLocaleString("pt-BR", { minimumFractionDigits: 2 })}
    </span>
  </div>
)}
```

```tsx
<span data-testid="checkout-total">
  R$ {total.toLocaleString("pt-BR", { minimumFractionDigits: 2 })}
</span>
```

Nas **duas** chamadas de `<OrderSummary …>` (a de desktop em `:542` e a mobile, se houver outra — procure por `OrderSummary` no arquivo e ajuste todas):

```tsx
<OrderSummary
  product={product}
  savings={savings}
  bump={bumpAtivo && bumpProduct ? { title: bumpProduct.title, price: bumpProduct.price } : null}
  total={total}
/>
```

No corpo enviado (`:122-128`) e no analytics (`:156`):

```tsx
const body: Record<string, unknown> = {
  productSlug: slug,
  email: form.email.trim().toLowerCase(),
  name: form.name.trim(),
  userId: user?.id ?? null,
  paymentMethod,
  // So o booleano: o preco do bump e recalculado no servidor, nunca aceito
  // do cliente.
  bump: bumpAtivo,
};
```

```tsx
amount: total,
```

- [ ] **Step 6: Rodar os testes novos + os antigos**

```bash
NODE_OPTIONS=--max-old-space-size=4096 npx vitest run src/pages/__tests__/CheckoutPage.bump.test.tsx src/pages/__tests__/CheckoutPage.test.tsx
```

Esperado: PASS nos 6 novos **e** nos 14 antigos. Se algum dos 14 quebrar, a regressão é sua — conserte antes de seguir.

- [ ] **Step 7: Lint e typecheck**

```bash
npx eslint src/lib/bump.ts src/pages/CheckoutPage.tsx src/pages/__tests__/CheckoutPage.bump.test.tsx
npx tsc -p tsconfig.app.json 2>&1 | grep -c "error TS"
```

Esperado: eslint limpo; contagem do tsc **igual a 35** (baseline pré-existente medido em 14/08). Qualquer número acima disso é erro novo seu.

- [ ] **Step 8: Commit**

```bash
git add src/pages/CheckoutPage.tsx src/pages/__tests__/CheckoutPage.bump.test.tsx
git commit -m "feat(bump): caixa marcavel no checkout com total somado"
```

---

## Task 4: `checkout-session` cobra o total e grava os itens

**Files:**
- Modify: `supabase/functions/checkout-session/index.ts` (destructure em `:190`; busca do produto em `:227-233`; insert do pedido em `:250-262`; chamada do Asaas em `:284-292`)

**Interfaces:**
- Consumes: `bump: boolean` do corpo (Task 3); tabela `order_items` (Task 1).
- Produces: pedido com `amount = total`, `product_id = principal`, e 1–2 linhas em `order_items`. Task 5 lê essas linhas.

**Sem teste automatizado** (não há harness Deno no repo). Verificação por revisão + o checklist de Step 4.

- [ ] **Step 1: Revalidar o bump no servidor e calcular o total**

Adicionar `bump` ao destructure de `:190`:

```ts
const { productSlug, email, name, paymentMethod, cpfCnpj, bump } = await req.json();
```

Logo depois da validação do preço do produto principal (`:244-247`), inserir:

```ts
// ── Bump de R$ 27 ──
// A TRAVA REAL mora aqui: o cliente manda so um booleano; o preco e a
// elegibilidade sao lidos do banco. `status = disponivel` repete a regra dura
// do catalogo — produto em_breve nunca e cobrado, nem como bump.
const BUMP_SLUG = "mapa-dos-sentimentos";
let bumpProduct: { id: string; title: string; price: number } | null = null;

if (bump === true && productSlug !== BUMP_SLUG) {
  const { data: bumpRow } = await supabaseAdmin
    .from("products")
    .select("id, title, price")
    .eq("slug", BUMP_SLUG)
    .eq("is_active", true)
    .eq("status", "disponivel")
    .maybeSingle();

  if (bumpRow) {
    const bumpPrice = typeof bumpRow.price === "number" ? bumpRow.price : parseFloat(String(bumpRow.price));
    if (isFinite(bumpPrice) && bumpPrice > 0) {
      bumpProduct = { id: bumpRow.id, title: bumpRow.title, price: bumpPrice };
    }
  }
  // Bump pedido mas indisponivel: segue SEM bump, cobrando so o principal.
  // Nunca cobra por algo que nao pode liberar.
  if (!bumpProduct) console.warn(`Bump solicitado mas indisponivel — seguindo sem bump | order de ${productSlug}`);
}

// Centavos para evitar 47.9 + 27.5 = 75.39999999999999
const totalAmount = Math.round((productPrice + (bumpProduct?.price ?? 0)) * 100) / 100;
```

- [ ] **Step 2: Cobrar o total e gravar os itens**

No insert do pedido (`:257`), trocar `amount: productPrice` por:

```ts
        amount: totalAmount,
```

Logo **depois** do bloco que confirma a criação do pedido (`:264-269`) e **antes** da integração com o Asaas (`:271`), inserir:

```ts
    // 2b. Itens do pedido. Gravados ANTES de cobrar: se falhar com bump,
    // ninguem e cobrado por um item que nao seria liberado.
    const itens = [
      { order_id: order.id, product_id: product.id, unit_price: productPrice },
      ...(bumpProduct ? [{ order_id: order.id, product_id: bumpProduct.id, unit_price: bumpProduct.price }] : []),
    ];

    const { error: itemsError } = await supabaseAdmin.from("order_items").insert(itens);

    if (itemsError) {
      console.error("order_items insert failed:", itemsError);
      if (bumpProduct) {
        // Com bump, a falha e fatal: cobrar dois e liberar um e pior que nao vender.
        await supabaseAdmin.from("orders").update({ status: "failed" }).eq("id", order.id);
        return jsonResponse(req, 500, { error: "Erro ao registrar os itens do pedido" });
      }
      // Sem bump, e recuperavel: a liberacao cai no fallback de orders.product_id.
      console.warn("Seguindo sem order_items — fallback por orders.product_id cobre este pedido");
    }
```

- [ ] **Step 3: Cobrar o valor certo no gateway**

Na chamada `createAsaasPayment` (`:284-292`), trocar o argumento de valor e a descrição:

```ts
        asaasData = await createAsaasPayment(
          asaasApiKey,
          customerId,
          totalAmount,
          paymentMethod || "pix",
          bumpProduct
            ? `${product.title} + ${bumpProduct.title} — Despertar Espiral`
            : `${product.title} — Despertar Espiral`,
          order.id,
          dueDateStr
        );
```

E no bloco do Sequenzy (`:325`), trocar `productPrice` por `totalAmount` no `amountFmt`:

```ts
      const amountFmt = `R$ ${totalAmount.toFixed(2).replace(".", ",")}`;
```

- [ ] **Step 4: Verificar por leitura (checklist)**

Rodar e conferir cada linha do resultado:

```bash
grep -n "totalAmount\|productPrice\|order_items\|BUMP_SLUG" supabase/functions/checkout-session/index.ts
```

Confirmar, item por item:
1. `productPrice` só aparece na validação do produto, no `unit_price` do item principal e no cálculo de `totalAmount`. **Nenhum** `amount:` ou chamada de gateway usa `productPrice` sozinho.
2. Todo caminho que cria `bumpProduct` passou pelos filtros `is_active` **e** `status = 'disponivel'`.
3. O insert de `order_items` acontece antes de `createAsaasPayment`.
4. Nenhum valor de preço vem de `req.json()`.

- [ ] **Step 5: Commit**

```bash
git add supabase/functions/checkout-session/index.ts
git commit -m "feat(bump): checkout-session revalida o bump e grava order_items"
```

---

## Task 5: Liberar os dois produtos

**Files:**
- Create: `supabase/functions/_shared/orderItems.ts`
- Modify: `supabase/functions/asaas-webhook/index.ts:149-183`
- Modify: `supabase/functions/grant-pending-access/index.ts:72-131`

**Interfaces:**
- Consumes: `order_items` (Task 1), gravado por Task 4.
- Produces: `produtosDoPedido(supabase, order): Promise<string[]>` — ids únicos dos produtos de um pedido, com fallback em `order.product_id`.

- [ ] **Step 1: Criar o helper compartilhado**

Criar `supabase/functions/_shared/orderItems.ts`:

```ts
/**
 * Produtos de um pedido, com fallback.
 *
 * Pedido novo tem linhas em `order_items` (principal + bump). Pedido antigo
 * nao tem nenhuma — e para esse caso existe o fallback em `orders.product_id`,
 * que mantem TODO pedido anterior ao bump funcionando exatamente como antes.
 */
// deno-lint-ignore no-explicit-any
export async function produtosDoPedido(supabase: any, order: { id: string; product_id?: string | null }): Promise<string[]> {
  const { data: itens, error } = await supabase
    .from("order_items")
    .select("product_id")
    .eq("order_id", order.id);

  if (error) {
    console.error("[order_items] leitura falhou, usando fallback:", error.message);
  }

  const ids = ((itens ?? []) as { product_id: string }[])
    .map((i) => i.product_id)
    .filter(Boolean);

  if (ids.length > 0) return Array.from(new Set(ids));
  return order.product_id ? [order.product_id] : [];
}
```

- [ ] **Step 2: `asaas-webhook` libera N produtos**

Import no topo do arquivo:

```ts
import { produtosDoPedido } from "../_shared/orderItems.ts";
```

Substituir o bloco de liberação (`:149-183`) por:

```ts
  const idsParaLiberar = await produtosDoPedido(supabase, order);

  async function liberarPara(userId: string): Promise<boolean> {
    // Um upsert por produto: um erro em um item nao impede a liberacao do outro.
    let algumOk = false;
    for (const productId of idsParaLiberar) {
      const { error: accessErr } = await supabase
        .from("user_products")
        .upsert({ user_id: userId, product_id: productId }, { onConflict: "user_id,product_id" });
      if (accessErr) console.error(`Failed to grant access product=${productId}:`, accessErr.message);
      else algumOk = true;
    }
    return algumOk;
  }

  if (order.user_id) {
    accessGranted = await liberarPara(order.user_id);
    if (accessGranted) console.log(`Access granted: user=${order.user_id} | products=${idsParaLiberar.length}`);
  } else {
    const { data: profile } = await supabase
      .from("user_profiles")
      .select("id")
      .eq("email", order.email.toLowerCase())
      .maybeSingle();

    if (profile?.id) {
      accessGranted = await liberarPara(profile.id);
      if (accessGranted) {
        await supabase.from("orders").update({ user_id: profile.id }).eq("id", order.id);
        grantedUserId = profile.id;
        console.log(`Access granted (email lookup): user=${profile.id} | products=${idsParaLiberar.length}`);
      }
    } else {
      console.warn(`No profile found for ${order.email} — access pending account creation`);
    }
  }
```

O bloco do Sequenzy que busca o produto por `order.product_id` (`:191-198`) **fica como está**: o e-mail de compra fala do produto principal, e mudar isso é outra decisão de conteúdo.

- [ ] **Step 3: `grant-pending-access` libera N produtos**

Import no topo:

```ts
import { produtosDoPedido } from "../_shared/orderItems.ts";
```

Trocar a query (`:72-76`) para trazer os itens junto, em uma consulta só:

```ts
  const { data: paidOrders, error: ordersErr } = await supabase
    .from("orders")
    .select("id, product_id, user_id, name, amount, payment_method, paid_at, products(title, slug), order_items(product_id, products(title, slug))")
    .eq("email", email)
    .eq("status", "paid");
```

Substituir a dedupe por pedido (`:96-104`) por dedupe **por produto**, achatando os itens:

```ts
  /* ── 5. Achatar pedidos em produtos (bump: um pedido pode ter dois) ── */
  interface ItemLiberavel { productId: string; title: string; slug: string; order: Record<string, unknown> }

  const seen = new Set<string>();
  const toGrant: ItemLiberavel[] = [];

  for (const order of paidOrders) {
    const itens = (order.order_items ?? []) as { product_id: string; products?: { title?: string; slug?: string } | null }[];

    // Pedido antigo (sem itens) cai no produto principal — comportamento intacto.
    const candidatos: ItemLiberavel[] = itens.length > 0
      ? itens.filter((i) => i.product_id).map((i) => ({
          productId: i.product_id,
          title: i.products?.title ?? "Produto",
          slug:  i.products?.slug  ?? "",
          order: order as Record<string, unknown>,
        }))
      : (order.product_id
          ? [{
              productId: order.product_id as string,
              title: (order.products as { title?: string } | null)?.title ?? "Produto",
              slug:  (order.products as { slug?: string  } | null)?.slug  ?? "",
              order: order as Record<string, unknown>,
            }]
          : []);

    for (const c of candidatos) {
      if (alreadyGranted.has(c.productId) || seen.has(c.productId)) continue;
      seen.add(c.productId);
      toGrant.push(c);
    }
  }
```

No laço de liberação (`:115-131`), trocar a origem dos três valores:

```ts
  for (const item of toGrant) {
    const productId    = item.productId;
    const productTitle = item.title;
    const productSlug  = item.slug;
```

O resto do laço (upsert, `continue` em erro, push em `grantedProducts`) **não muda**. Confira o restante do laço depois de `:131` e ajuste qualquer referência remanescente a `order.` para `item.order.`.

**Nota:** `produtosDoPedido` não é usado aqui — a query com join resolve em uma consulta só, sem N+1. O helper existe para o `asaas-webhook`, que trabalha com um pedido de cada vez. Se o join embutido `order_items(product_id, products(title, slug))` não resolver no PostgREST desta instância (nome da FK), caia para `produtosDoPedido` por pedido e busque os títulos em uma segunda consulta — e registre a troca no report.

- [ ] **Step 4: Verificar por leitura (checklist)**

```bash
grep -n "product_id\|produtosDoPedido\|toGrant" supabase/functions/asaas-webhook/index.ts supabase/functions/grant-pending-access/index.ts
```

Confirmar:
1. Nenhum `upsert` de `user_products` usa `order.product_id` diretamente — todos passam pela lista.
2. O fallback para pedido sem itens existe nos **dois** arquivos.
3. Erro em um produto não aborta o outro.

- [ ] **Step 5: Rodar a suíte inteira (gate de regressão)**

```bash
NODE_OPTIONS=--max-old-space-size=4096 npm test 2>&1 | node C:\Users\Thales\.claude\bin\lean.mjs --quiet
```

Esperado: **33 falhas pré-existentes**, nem uma a mais. As 33 são de mocks, documentadas em `.superpowers/sdd/diagnostico-suite.md`. Qualquer falha nova é sua.

- [ ] **Step 6: Commit**

```bash
git add supabase/functions/_shared/orderItems.ts supabase/functions/asaas-webhook/index.ts supabase/functions/grant-pending-access/index.ts
git commit -m "feat(bump): liberar todos os produtos do pedido, com fallback"
```

---

## Riscos e decisões que não são do implementador

1. **O bump está `em_breve`** — a caixa não aparece até a Sunyan entregar o conteúdo (vídeo + áudio, spec §5) e alguém mudar o status. Hoje isso é **SQL manual**: o admin não tem controle de status (pendência I6 herdada da leva da vitrine).
2. **Gateway.** A spec §9 avisa: se a troca Asaas → Woovi for feita, deveria vir **antes** do bump. Medido hoje: este repo está em Asaas e não há Woovi em andamento aqui (o Woovi vive no outro codebase, o PHP). O risco de refazer trabalho é baixo — `order_items` é agnóstico de gateway e só a chamada de cobrança mudaria. Decisão do Thales.
3. **Reembolso parcial** (devolver só o bump) não existe neste plano e não existe hoje no sistema. Fora de escopo.
4. **O e-mail de compra fala só do produto principal.** Mencionar o bump é decisão de conteúdo, não de código.

## Gates de merge (somam-se aos que já existem na branch)

1. Aplicar `20260814_000001_order_items.sql` — **produção só com aprovação explícita do Thales**.
2. Deploy de `checkout-session`, `asaas-webhook`, `grant-pending-access` — os três juntos: gravar itens sem saber liberá-los deixa a aluna pagando por dois e recebendo um.
3. Produto do bump em `disponivel` **só depois** do conteúdo real existir.

---

## Self-Review

**Cobertura da spec §3.3 e §4:** caixa marcável antes do botão (Task 3) ✓ · total muda ao marcar (Task 3) ✓ · dois produtos liberados no pagamento confirmado (Task 5) ✓ · `order_items` mantendo `orders.product_id` preenchido (Tasks 1 e 4) ✓ · liberação percorre `order_items` quando existirem (Task 5) ✓ · RLS na tabela nova (Task 1) ✓.

**Cobertura dos testes exigidos pela spec §8:** "total com e sem bump" → Task 2 (`calcularTotal`) + Task 3 (UI, 47 → 74) ✓ · "pedido com dois itens libera dois produtos" → Task 5 ✓ (verificação por leitura: não há harness Deno neste repo — limitação declarada, não omitida) · "pedido sem bump continua liberando um" → fallback da Task 5 + caso `bump:false` da Task 3 ✓.

**Consistência de tipos:** `bumpOferecivel`/`calcularTotal`/`BUMP_SLUG`/`ProdutoBump` definidos na Task 2 e consumidos com a mesma assinatura na Task 3 ✓ · a chave `bump` do corpo é produzida na Task 3 e lida na Task 4 com o mesmo nome ✓ · `order_items(order_id, product_id, unit_price)` criada na Task 1, escrita na Task 4 e lida na Task 5 com os mesmos nomes de coluna ✓.

**Limitação declarada:** Tasks 1, 4 e 5 não têm teste automatizado porque o repo não tem harness de SQL nem de Deno. Cada uma tem checklist de verificação por leitura em vez de um `expect`. Isso é uma fraqueza real deste plano, não um descuido — quem revisar deve ler o diff dessas três com atenção redobrada.
